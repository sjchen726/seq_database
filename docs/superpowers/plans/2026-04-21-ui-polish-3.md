# UI Polish Sprint 3 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add frontend table sorting to all 5 list pages, unify delete button styling with edit button, and center the "Sequences" column header in seq_list.

**Architecture:** CSS-first — new `.ds-th-sort` utility class controls sort appearance; a single new `table-sort.js` auto-discovers all `th.ds-th-sort` elements and attaches click handlers. Delete button fix is a CSS simplification + HTML class addition. Sequences header is a one-line HTML change.

**Tech Stack:** Vanilla JS (ES5), CSS, Django templates. Worktree: `.worktrees/frontend-redesign`. No Python changes.

---

## File Map

| File | Action | What changes |
|---|---|---|
| `static/css/design-system.css` | Modify (lines 834–839) | Replace `.ds-act-delete` block; append `.ds-th-sort` CSS |
| `static/js/table-sort.js` | **Create** | Generic table sort handler |
| `templates/base.html` | Modify (line 127) | Add `table-sort.js` script tag after `loading.js` |
| `templates/seq_list.html` | Modify (lines 180–199) | Add `ds-th-sort` to 10 `<th>` elements; center Sequences `<th>` |
| `templates/module_list.html` | Modify (lines 23–26, 41) | Add `ds-th-sort` to 3 `<th>`; add `ds-act` to delete button |
| `templates/seqmodule_list.html` | Modify (lines 21–24, 53) | Add `ds-th-sort` to 3 `<th>`; add `ds-act` to delete button |
| `templates/reg_seq_list.html` | Modify (lines 25–31) | Add `ds-th-sort` to 5 `<th>` |
| `templates/auth_list.html` | Modify (lines 17–22) | Add `ds-th-sort` to 5 `<th>` |

---

## Task 1: CSS — sort icons + delete button fix

**Files:**
- Modify: `.worktrees/frontend-redesign/static/css/design-system.css:834–839`

- [ ] **Step 1: Replace `.ds-act-delete` block (lines 834–839)**

Current block at lines 834–839:
```css
.ds-act-delete {
  background: #fff; border: 1px solid #fca5a5; color: #ef4444;
  border-radius: 5px; padding: 3px 10px; font-size: 12px;
  cursor: pointer; transition: background .12s, border-color .12s;
}
.ds-act-delete:hover { background: #fef2f2; border-color: #f87171; }
```

Replace with (color-only — structure comes from `.ds-act`):
```css
.ds-act-delete { background: #fef2f2; border-color: #fca5a5; color: #ef4444; }
.ds-act-delete:hover { background: #fee2e2; border-color: #f87171; }
```

- [ ] **Step 2: Append sort CSS at end of file**

Add at the very end of `design-system.css`:
```css
/* ── Sortable table header ── */
.ds-th-sort {
  cursor: pointer; user-select: none; white-space: nowrap;
}
.ds-th-sort:hover { background: #f1f5f9; }
.ds-th-sort .ds-sort-icon {
  font-size: 11px; color: #94a3b8; margin-left: 3px;
}
.ds-th-sort.ds-sort-asc,
.ds-th-sort.ds-sort-desc { color: #1d4ed8; }
.ds-th-sort.ds-sort-asc .ds-sort-icon,
.ds-th-sort.ds-sort-desc .ds-sort-icon { color: #3b82f6; }
```

- [ ] **Step 3: Commit**

```bash
cd /Users/gutou/Projects/seq_web/seq_database_v2/.worktrees/frontend-redesign
git add static/css/design-system.css
git commit -m "style: fix ds-act-delete to extend ds-act base; add ds-th-sort CSS"
```

---

## Task 2: Create table-sort.js + add to base.html

**Files:**
- Create: `.worktrees/frontend-redesign/static/js/table-sort.js`
- Modify: `.worktrees/frontend-redesign/templates/base.html:127`

- [ ] **Step 1: Create `static/js/table-sort.js`**

Create the file with this exact content:
```javascript
(function () {
  document.querySelectorAll('table').forEach(function (table) {
    var headers = table.querySelectorAll('th.ds-th-sort');
    if (!headers.length) return;
    var state = { col: -1, dir: 'asc' };

    headers.forEach(function (th) {
      var icon = document.createElement('span');
      icon.className = 'ds-sort-icon';
      icon.textContent = '↕';
      th.appendChild(icon);

      th.addEventListener('click', function () {
        var colIdx = Array.from(th.parentNode.children).indexOf(th);
        var newDir = (state.col === colIdx && state.dir === 'asc') ? 'desc' : 'asc';

        headers.forEach(function (h) {
          h.classList.remove('ds-sort-asc', 'ds-sort-desc');
          h.querySelector('.ds-sort-icon').textContent = '↕';
        });

        th.classList.add(newDir === 'asc' ? 'ds-sort-asc' : 'ds-sort-desc');
        th.querySelector('.ds-sort-icon').textContent = newDir === 'asc' ? '↑' : '↓';
        state = { col: colIdx, dir: newDir };

        var tbody = table.querySelector('tbody');
        var rows = Array.from(tbody.querySelectorAll('tr'));
        rows.sort(function (a, b) {
          var av = (a.cells[colIdx] ? a.cells[colIdx].textContent : '').trim();
          var bv = (b.cells[colIdx] ? b.cells[colIdx].textContent : '').trim();
          var an = parseFloat(av), bn = parseFloat(bv);
          if (!isNaN(an) && !isNaN(bn)) return newDir === 'asc' ? an - bn : bn - an;
          return newDir === 'asc' ? av.localeCompare(bv) : bv.localeCompare(av);
        });
        rows.forEach(function (r) { tbody.appendChild(r); });
      });
    });
  });
})();
```

- [ ] **Step 2: Add script tag to `base.html` after `loading.js` (line 127)**

Current line 127:
```html
<script src="/static/js/loading.js"></script>
```

Add immediately after it:
```html
<script src="/static/js/table-sort.js"></script>
```

Result (lines 127–128):
```html
<script src="/static/js/loading.js"></script>
<script src="/static/js/table-sort.js"></script>
```

- [ ] **Step 3: Commit**

```bash
cd /Users/gutou/Projects/seq_web/seq_database_v2/.worktrees/frontend-redesign
git add static/js/table-sort.js templates/base.html
git commit -m "feat: add table-sort.js global sort handler"
```

---

## Task 3: seq_list.html — sort headers + Sequences th centered

**Files:**
- Modify: `.worktrees/frontend-redesign/templates/seq_list.html:180–199`

Current thead (lines 179–200):
```html
<th><input type="checkbox" id="select-all"></th>
<th>Strand ID</th>
<th>Project</th>
<th>Target</th>
<th>Sequence ID</th>
<th>Ligand 1</th>
<th>
  Sequences<br>
  <select id="seq_type_selector" class="ds-seq-type-selector">
    ...
  </select>
</th>
<th>Ligand 2</th>
<th>Transcript</th>
<th>Position</th>
<th>Strand_MWs</th>
<th>Parents</th>
<th>Remarks</th>
<th>Update Time</th>
<th>操作</th>
```

- [ ] **Step 1: Add `ds-th-sort` to sortable headers + center Sequences th**

Replace the thead content with:
```html
<th><input type="checkbox" id="select-all"></th>
<th class="ds-th-sort">Strand ID</th>
<th class="ds-th-sort">Project</th>
<th class="ds-th-sort">Target</th>
<th class="ds-th-sort">Sequence ID</th>
<th>Ligand 1</th>
<th style="text-align:center;">
  Sequences<br>
  <select id="seq_type_selector" class="ds-seq-type-selector">
    ...
  </select>
</th>
<th>Ligand 2</th>
<th class="ds-th-sort">Transcript</th>
<th class="ds-th-sort">Position</th>
<th class="ds-th-sort">Strand_MWs</th>
<th class="ds-th-sort">Parents</th>
<th class="ds-th-sort">Remarks</th>
<th class="ds-th-sort">Update Time</th>
<th>操作</th>
```

Keep the `<select>` inner content exactly as it was — only the `<th>` wrapper gets `style="text-align:center;"`.

- [ ] **Step 2: Commit**

```bash
cd /Users/gutou/Projects/seq_web/seq_database_v2/.worktrees/frontend-redesign
git add templates/seq_list.html
git commit -m "feat: add sort headers to seq_list; center Sequences th"
```

---

## Task 4: module_list.html + seqmodule_list.html — sort headers + delete button

**Files:**
- Modify: `.worktrees/frontend-redesign/templates/module_list.html:23–26, 41`
- Modify: `.worktrees/frontend-redesign/templates/seqmodule_list.html:21–24, 53`

### module_list.html

- [ ] **Step 1: Update table headers (lines 23–26)**

Current:
```html
<th style="min-width:130px;">Keyword</th>
<th>Type Code</th>
<th style="min-width:80px;">Strand_MWs</th>
<th style="cursor:default;">操作</th>
```

Replace with:
```html
<th class="ds-th-sort" style="min-width:130px;">Keyword</th>
<th class="ds-th-sort">Type Code</th>
<th class="ds-th-sort" style="min-width:80px;">Strand_MWs</th>
<th>操作</th>
```

(Remove `style="cursor:default;"` from 操作 — it's now unnecessary since `.ds-th-sort` adds cursor, and non-sort headers use default cursor.)

- [ ] **Step 2: Update delete button (line 41)**

Current:
```html
<button type="submit" class="ds-act-delete">删除</button>
```

Replace with:
```html
<button type="submit" class="ds-act ds-act-delete">删除</button>
```

### seqmodule_list.html

- [ ] **Step 3: Update table headers (lines 21–24)**

Current:
```html
<th style="min-width:130px;">Keyword</th>
<th>Base Char</th>
<th style="min-width:90px;">Linker Connector</th>
<th style="cursor:default;">操作</th>
```

Replace with:
```html
<th class="ds-th-sort" style="min-width:130px;">Keyword</th>
<th class="ds-th-sort">Base Char</th>
<th class="ds-th-sort" style="min-width:90px;">Linker Connector</th>
<th>操作</th>
```

- [ ] **Step 4: Update delete button (line 53)**

Current:
```html
<button type="submit" class="ds-act-delete">删除</button>
```

Replace with:
```html
<button type="submit" class="ds-act ds-act-delete">删除</button>
```

- [ ] **Step 5: Commit**

```bash
cd /Users/gutou/Projects/seq_web/seq_database_v2/.worktrees/frontend-redesign
git add templates/module_list.html templates/seqmodule_list.html
git commit -m "feat: sort headers + fix delete button in module_list and seqmodule_list"
```

---

## Task 5: reg_seq_list.html + auth_list.html — sort headers

**Files:**
- Modify: `.worktrees/frontend-redesign/templates/reg_seq_list.html:25–31`
- Modify: `.worktrees/frontend-redesign/templates/auth_list.html:17–22`

### reg_seq_list.html

- [ ] **Step 1: Update table headers (lines 25–31)**

Current:
```html
<th class="cell-id">Strand ID</th>
<th>Sequences (5'→3')</th>
<th>Position</th>
<th>Transcript</th>
<th>Remarks</th>
<th style="white-space:nowrap;">Registration Time</th>
<th>操作</th>
```

Replace with:
```html
<th class="ds-th-sort cell-id">Strand ID</th>
<th>Sequences (5'→3')</th>
<th class="ds-th-sort">Position</th>
<th class="ds-th-sort">Transcript</th>
<th class="ds-th-sort">Remarks</th>
<th class="ds-th-sort" style="white-space:nowrap;">Registration Time</th>
<th>操作</th>
```

### auth_list.html

- [ ] **Step 2: Update table headers (lines 17–22)**

Current:
```html
<th class="cell-id">#</th>
<th>姓名</th>
<th>邮箱</th>
<th>角色</th>
<th>项目权限</th>
<th>操作</th>
```

Replace with:
```html
<th class="ds-th-sort cell-id">#</th>
<th class="ds-th-sort">姓名</th>
<th class="ds-th-sort">邮箱</th>
<th class="ds-th-sort">角色</th>
<th class="ds-th-sort">项目权限</th>
<th>操作</th>
```

- [ ] **Step 3: Commit**

```bash
cd /Users/gutou/Projects/seq_web/seq_database_v2/.worktrees/frontend-redesign
git add templates/reg_seq_list.html templates/auth_list.html
git commit -m "feat: add sort headers to reg_seq_list and auth_list"
```

---

## Self-Review

**Spec coverage:**
- ✅ Item 1 (sort): Tasks 1–5 cover CSS, JS, and all 5 templates
- ✅ Item 2 (delete button): Task 1 (CSS) + Task 4 (HTML ×2)
- ✅ Item 3 (Sequences th): Task 3

**Placeholder scan:** None found.

**Type consistency:**
- `ds-th-sort` class used consistently in CSS (Task 1) and all HTML tasks (3–5)
- `ds-sort-icon` span created by JS (Task 2) matches CSS selector `.ds-th-sort .ds-sort-icon` (Task 1)
- `ds-sort-asc` / `ds-sort-desc` toggled in JS (Task 2) match CSS selectors (Task 1)
- `ds-act ds-act-delete` in HTML (Task 4) relies on `.ds-act` base + `.ds-act-delete` color-only rules (Task 1)
