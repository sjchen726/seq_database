# ============================================================
# 远程服务器数据库配置备份（勿提交到 git）
# 恢复时将以下内容替换 settings.py 中的 DATABASES 块
# ============================================================

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'bms',
        'HOST': '127.0.0.1',   # 服务器本机地址（在服务器上运行时为本地）
        'PORT': 3306,
        'USER': 'root',
        'PASSWORD': 'Bt123456',
    }
}

# 备注：另一个密码备选（原代码注释中）
# 'PASSWORD': 'BTYY.com@db',
